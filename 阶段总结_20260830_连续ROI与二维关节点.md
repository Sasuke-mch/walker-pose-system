# 连续人体框与二维关节点阶段总结

本阶段首先确认了中近距离的主要问题不是所有姿态模型都无法识别脚部，而是原始 YOLO 人体框常在小腿或鞋部上方结束。把原框固定向外扩展能够恢复足部，但远距离会引入无关背景，固定比例不适合作为最终规则。

随后以当前 YOLO 框宽占画面宽度的比例 `r` 构造连续姿态输入框。`r <= 0.37` 时保持检测框；其余情况下采用 `g=((r-0.37)/(1-0.37))^0.75`，并向左右、上方、下方分别扩展 `0.20gw`、`0.08gh`、`1.30gh`。下方扩展更大，目的是优先保留鞋部与少量地面背景。72 对独立抽样图的视觉复核表明：远距离保持原框，中距离小幅增加脚下空间，近距离均延伸至画面下缘并完整包含可见鞋部。

在固定 60 对图像上，连续框重新运行了 PMPose-b、ProbPose-s 和 Sapiens2-0.4B。髋、膝、踝的误差相对经过视觉筛选的 Sapiens2 工程伪标签统计，不能称作人工真值准确率。

| 模型 | 旧宽度自适应框 | 连续框 | 结论 |
| --- | --- | --- | --- |
| PMPose-b | 100.0% 覆盖率，22.90 px，PCK@25 67.3% | 100.0%，20.38 px，72.2% | 平均偏差下降 11.0%；near 下降 18.7%，为当前主模型。 |
| ProbPose-s | 100.0%，26.29 px，62.2% | 100.0%，26.41 px，64.9% | PCK 提升，但平均偏差增加 0.5%，不宜为适配它继续增大框。 |
| YOLO26x-pose | 98.5%，101.98 px，8.9% | 无连续框版本 | 一阶段固定基线，用于方法比较，不参与框策略因果对照。 |

Sapiens2 的足部可见性由旧宽度框的 889/960 个可用点、84/120 张完整 8 点图，提高为连续框的 960/960、120/120；near 由 249/320 个点、4/40 张完整提升为 320/320、40/40。这支持“连续框消除了输入截断造成的脚部缺失”，不证明足趾和脚跟已经达到人工像素精度。

当前决策是保留 YOLO26 作为人体检测器，采用连续框作为 PMPose 的姿态输入策略；YOLO26x-pose 保留为固定一阶段基线；BBoxMaskPose 暂停投入。不能因为 PMPose 的开发集结果更好，就认定连续框已经可部署。下一项有效实验是冻结函数并在另一采集会话上复测 PMPose 的 far 不退化、near 改善和足部完整率。

相关完整结果：`screening/E20260830-A15_continuous_roi_2d_comparison/`。
