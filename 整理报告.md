# walker_pose_system 项目整理报告

**执行时间：** 2026-08-22 23:02 ~ 2026-08-23 00:45
**执行人：** QClaw
**根目录：** `D:\my_works\walker_pose_system\`

---

## 一、整理结果概览

| 指标 | 整理前 | 整理后 | 变化 |
|------|--------|--------|------|
| 总体积 | 14.80 GB | 4.83 GB | **-9.97 GB（-67%）** |
| 文件数 | 8,820 | 5,030 | -3,790 |
| 代码文件 | 3 份重复 | 1 份唯一 | git 管理 |
| 模型权重 | 3 处重复 | 1 处统一 | `models/` |
| 版本控制 | 无 | git init + 首次提交 | 163 文件入库 |

---

## 二、整理后项目结构

```
D:\my_works\walker_pose_system\           # 单一大文件夹
├── .git/                                 # 版本控制（新建）
├── .gitignore                            # 忽略规则（新建）
├── 整理日志.txt                           # 详细操作日志
├── 整理报告.md                            # 本文件
│
├── realtime_app/                         # 核心开发线（唯一代码副本）
│   ├── pose_app/                         # 14 个模块
│   │   ├── config.py                     # 已修改硬路径
│   │   ├── triangulation.py              # 三角化
│   │   ├── stereo_camera.py              # 双目相机
│   │   ├── calibration.py                # 标定
│   │   ├── docker_service.py             # Docker 调用
│   │   └── ...
│   ├── server/                           # 4 个服务端
│   ├── tools/                            # 14 个工具脚本
│   ├── tests/                            # 4 个测试
│   ├── scripts/                          # 3 个脚本
│   ├── calibration/
│   │   └── results/                      # 标定最终参数（3 个 json）
│   │       ├── cam0_fisheye.json         # CAM0 内参
│   │       ├── cam1_fisheye.json         # CAM1 内参
│   │       └── stereo_fisheye.json       # 双目外参
│   ├── config.example.json               # 已修改硬路径
│   ├── run.py / run_stereo.py            # 入口
│   └── requirements.txt
│
├── walker-pose-system/                   # 发布版仓库（精简副本）
│   ├── README.md / CHANGELOG.md
│   ├── benchmarks/                       # 实验结果摘要
│   ├── sequence_pipeline/                # 序列实验代码
│   ├── realtime_app/                     # 发布版代码（硬路径已改）
│   └── docs/
│
├── models/                               # 模型权重统一（git 忽略）
│   ├── PMPose-b-1.0.0.pth               # 589.1 MB
│   ├── SAM-pose2seg_hiera_b+.pt         # 341.0 MB
│   └── yolo26x-pose.pt                   # 120.4 MB
│
├── third_party/                          # 第三方源码（git 忽略）
│   └── bboxmaskpose/                     # 含 mmpose、sam2 等完整源码
│
├── 资料/                                  # 非代码资产（git 忽略）
│   ├── 讲义/                             # 13 个 docx/pdf
│   ├── 论文/                             # 4 篇 PDF
│   ├── 发票/                             # 2 个文件
│   └── github_repo_organizer/            # 辅助工具
│
└── .idea/ .claude/                       # IDE 配置（git 忽略）
```

---

## 三、删除清单（完整记录）

### Phase 1 — A 级（零风险，临时/损坏文件）
| 操作 | 对象 | 大小 |
|------|------|------|
| 删除 | 20 个 `__pycache__/` 目录 | - |
| 删除 | `python_files.txt` `recent_files.txt` `configs.txt` `models.txt` | 31 KB |
| 删除 | 5 个空目录：`_archive/` `dataset/` `docs/` `notes/` `tools/` | 0 |
| 删除 | `cam0_intrinsic.zip`（损坏，0 字节实际） | 169 MB |
| 删除 | Word 锁文件 `~$thon语法...docx` | 162 B |
| 删除 | `walker-pose-system/realtime_app.zip` | 62 KB |

### Phase 2 — B 级（重复副本，保留最新）
| 操作 | 对象 | 大小 |
|------|------|------|
| 删除 | `pose_realtime_app/`（已被 realtime_app 取代） | 23.7 MB |
| 删除 | `pose_sequence_pipeline/`（已被 walker-pose-system 取代） | 251 KB |
| 删除 | `cam0_intrinsic_inspection/overlays/`（重复图） | 202.7 MB |
| 删除 | `cam1_intrinsic_inspection/overlays/`（重复图） | 176 MB |
| 删除 | `cam1_intrinsic_exclude_bad/`（重复图） | 171.4 MB |
| 删除 | 6 个 `.bak` 备份文件 | 144.9 KB |

### Phase 3 — C 级（标定数据已算完、实验产物已提取摘要）
| 操作 | 对象 | 大小 |
|------|------|------|
| 删除 | `calibration/captures/`（标定原始图，结果已算完） | 2.11 GB |
| 删除 | `outputs/stereo_capture/`（标定录制视频） | 1.77 GB |
| 删除 | `outputs/` 空目录 | 0 |
| 删除 | `experiments/`（3.9 GB 实验产物，摘要已进 benchmarks） | 3.90 GB |

### Phase 4 — D/E 级（移动，未删除）
| 操作 | 源 → 目标 | 大小 |
|------|-----------|------|
| 移动 | `documents/` → `资料/讲义/` | 7.3 MB |
| 移动 | `papers/` → `资料/论文/` | 21.9 MB |
| 移动 | `发票/` → `资料/发票/` | 299 KB |
| 移动 | `github_repo_organizer/` → `资料/github_repo_organizer/` | 14.8 KB |
| 移动 | `third_party_models/` → `third_party/` | 3.77 GB |
| 复制+删除 | `model_cache/` + `docker_cache/` → `models/`（去重后） | 1.03 GB 净 |

### Phase 5 — 硬路径修改
| 文件 | 原路径 | 新路径 |
|------|--------|--------|
| `realtime_app/config.example.json` | `../model_cache/pmpose` | `../models` |
| `realtime_app/pose_app/config.py` | `pose_realtime_app/outputs` | `realtime_app/outputs` |
| `realtime_app/pose_app/config.py` | `model_cache/pmpose` | `../models` |
| `walker-pose-system/realtime_app/config.example.json` | `../model_cache/pmpose` | `../../models` |
| `walker-pose-system/realtime_app/pose_app/config.py` | `pose_realtime_app/outputs` | `realtime_app/outputs` |
| `walker-pose-system/realtime_app/pose_app/config.py` | `model_cache/pmpose` | `../../models` |

---

## 四、保留的关键资产清单

### 代码（git 跟踪，163 文件）
- `realtime_app/` — 14 个 pose_app 模块 + 4 个 server + 14 个 tools + 4 个 tests + 3 个 scripts
- `walker-pose-system/` — 发布版（README + CHANGELOG + benchmarks + sequence_pipeline + realtime_app）

### 标定参数（已入库）
- `realtime_app/calibration/results/cam0_fisheye.json` — CAM0 鱼眼内参
- `realtime_app/calibration/results/cam1_fisheye.json` — CAM1 鱼眼内参
- `realtime_app/calibration/results/stereo_fisheye.json` — 双目外参（基线 426.643mm，光轴夹角 77.432°）

### 模型权重（git 忽略，本地保留）
- `models/PMPose-b-1.0.0.pth` — 589.1 MB
- `models/SAM-pose2seg_hiera_b+.pt` — 341.0 MB
- `models/yolo26x-pose.pt` — 120.4 MB

### 第三方源码（git 忽略，本地保留）
- `third_party/bboxmaskpose/` — 含 mmpose 整库、sam2 源码、api.py 等

### Docker 镜像（12 个，完好）
- yolo26-realtime/test/test-fixed、sapiens2、bboxmaskpose、pytorch 2.7/2.4、nvidia/cuda 系列、ursim
- docker_data.vhdx 在 `D:\Docker\wsl\disk\`（之前已迁移）

### 实验摘要（已入库）
- `walker-pose-system/benchmarks/accuracy/` — 7 个 CSV/JSON 精度对比
- `walker-pose-system/benchmarks/speed/` — 7 个 CSV 速度对比
- `walker-pose-system/benchmarks/qualitative/` — 5 张可视化对比图

---

## 五、项目可运行性验证

| 检查项 | 结果 |
|--------|------|
| git init + commit | OK（5f7dec8, 163 files, 25283 insertions） |
| `pose_app.config` import | OK（硬路径修改生效） |
| Docker Desktop 连接 | OK |
| Docker 镜像（12 个） | 全部完好 |
| 模型权重（3 个文件 1.03GB） | 在 `models/` 可挂载 |
| 第三方源码 | 在 `third_party/` 可挂载 |
| 标定参数 | 在 `calibration/results/` 可读取 |

**运行方式不变**：通过 Docker 容器挂载 `models/` 和 `third_party/` 到容器内，代码里 `config.example.json` 已指向新路径。

---

## 六、后续建议

1. **不要再手工 .bak 备份** — 已用 git，需要时 `git stash` 或分支
2. **新代码写到 `realtime_app/`** — 这是唯一开发线
3. **`walker-pose-system/` 只在发布时同步** — 不要在里面直接开发
4. **模型权重/第三方源码不要进 git** — 已在 .gitignore 排除
5. **标定原始数据已删除** — 如需重新标定，用 `tools/calibrate_stereo_fisheye.py` 重新拍摄计算
6. **推到远程仓库前** — 先检查 `git status`，确认没有误加 models/ 或 third_party/
