# Walker Pose System - Claude 工作指南

## 项目概述

面向助步器场景的人体姿态估计与实时可视化研究代码。核心任务是在鱼眼双目立体视觉系统中准确识别下肢关节点，特别是脚部关键点，用于步态分析。

## 核心技术栈

- **相机系统**: 双鱼眼相机，ChArUco标定，真实采集配对回放
- **2D姿态估计**: YOLO26x-pose（端到端）、YOLO26x + PMPose（两阶段）、Sapiens2-308点（包含脚部细节）
- **3D重建**: 基于鱼眼标定的双目三角化（B0正式基线）
- **深度辅助**: DA3深度估计（相对深度，非米制，用于图像处理辅助）

## 当前研究状态（2026-09-04主线）

当前最高优先级是先把端到端技术链做通，再做局部优化。现有双目输入、二维姿态、原始鱼眼坐标恢复、单人关联、三角化和逐帧保存作为前半链；下一步从严格三角化结果接统一下肢时序接口，随后依次接固定坐标、基础运动学、步态事件候选和统一实时输出。相机前伸架、局部投影、门限和滤波优化在最小闭环完成前不得取代主线。

T1 统一下肢时序接口、T2 基础运动学、T3 固定坐标、T4 非接触转折点候选和 T5 统一输出的软件路径均已用 C3 保存的 60 对严格几何结果贯通。`run_stereo.py --enable-lower-limb-pipeline` 会在同一运行目录写出 T1–T4，并拒绝与 `--no-json` 同用；它还会逐对写入 `lower_limb_live_status.jsonl`，仅报告已经完成严格三角化后的当前状态和经下一对确认的三帧候选，并将同一记录的关节有效数、膝角可用性、T3 状态和非接触候选数叠加到预览/标注视频。该叠加绝不反向改变二维、关联或几何。T3 当前只验证了受状态保护的非单位测试变换、双坐标输出和数值可逆性；T4 只输出严格三相邻帧的原始运动学局部极值，绝不标记触地、离地、支撑、摆动或周期。硬件安装固定后仍须建立 `measured_locked` 的左相机到助步器/地面坐标变换并检查静态稳定性；在此之前不输出步长、步宽、足地高度或正式步态事件标签。

硬件接入前先运行 `tools/preflight_stereo_lower_limb_pipeline.py`。它仅检查本机配置、模型路径、端口号、标定、相机注册 JSON 和可选变换的状态，不枚举/打开相机、不检查 Docker/GPU/服务，也不进行推理；`software_ready` 仅表示配置自洽，不是现场运行通过。

### 已验证的工程基线

1. **B0基线**: 当前正式基线，直接在鱼眼原图上进行2D检测+三角化
2. **PMPose**: 当前髋、膝、踝的工程二维基线（相对Sapiens2伪标签）
3. **Sapiens2-308**: 可提供脚踝、大脚趾、小脚趾和脚跟候选点

### 已知负结果

- 局部虚拟视角：收益极小且显著降速
- 模型输入去畸变：为负结果

### 当前痛点

1. **检测框截断问题**: 正向YOLO框在中近距离可能截断双脚，导致脚部关键点不可见
2. **下肢三维链不连续**: 近距离单侧二维脚点可见，但左右关联后的完整下肢三维链仍不连续
3. **重点方向**: 先完成三维候选到时序、运动学、步态候选和统一输出的最小闭环；已有局部问题保留为闭环后的优化清单

### 当前基线验证范围（2026-09-02）

- 不再扩展 DA3 实验。冻结单人二维比较以 PMPose-b、ProbPose-s 共享 C3 原始鱼眼输入与旋转；每张图只选最高 detector bbox score 的一名人体，其他检测框作为 false-positive 审计保留。
- 在 `V20260904_sapiens2_reference_single_person_2d_comparison` 中，Sapiens2-0.4B 是用户规定的二维操作性参考，PMPose/ProbPose 只报告对该参考的像素差与覆盖；Sapiens2 不参加自身评分，且任何结果不得升级为外部人体二维或三维准确率。
- 原始范围外的二维点保留在审计输出，但在共享标定几何入口以 `out_of_raw_image_bounds` 拒绝；不得裁剪、回拉或改记为低二维分数。

### DA3的定位

- 已在原始鱼眼图上验证可提供**相对深度层次**和**遮挡诊断信息**
- **不是米制深度**，不替代鱼眼标定、左右匹配或三角化
- 目前仅保留为**逐帧、图像空间的 ROI / 遮挡诊断辅助先验**；每对同步图像必须独立运行 DA3
- 不得用 DA3 决定相机内外参、人物关联、关键点筛选或三角化；历史“选择性去畸变”混合像素不是单一可逆相机投影，已排除出主线
- D1 原始鱼眼安全 ROI 相对冻结 C3 的 60 对保存预测回放未通过替代条件：关联保持 60/60，但下肢有效点 `291→286`，共同可计算点的未筛选残差中位数 `3.108→3.746 px`。D1 不进入主线；该结果不是二维或三维准确率。

## 代码组织

```
walker-pose-system/
├─ realtime_app/              # 单路实时姿态与双目三角化基线程序
│  ├─ pose_app/              # 核心模块
│  │  ├─ calibration.py     # 双目标定
│  │  ├─ model_undistort.py # 鱼眼去畸变（已验证为负结果）
│  │  ├─ local_perspective.py # 局部虚拟视角（收益极小）
│  │  ├─ triangulation.py   # 三角化核心
│  │  └─ rotation.py        # 图像旋转与逆映射
│  ├─ tools/                 # 实验工具脚本
│  └─ run_stereo.py         # 双目主程序
├─ research_records/         # 本地研究档案（不提交）
│  ├─ raw_captures/         # 原始采集
│  ├─ official_runs/        # 正式实验
│  └─ engineering_validation/ # 工程验证
└─ docs/                     # 项目状态与工程说明
```

## 实验记录规则

**禁止写入哈希值**（自2026-09-01起）。实验可追溯性应通过：
- 明确的输入路径
- 数据范围
- 代码文件
- 参数
- 输出路径
- 结果表
- 结论边界

## 工作原则

### 1. 响应中文请求

用户使用中文，响应也应使用中文。

### 2. 小修改优先

- 项目已有相对成熟的方法
- 优先考虑在现有方法基础上的小修改
- 大幅度重构需要明确讨论

### 3. 深度图应用边界

DA3深度图的使用必须遵循：
- 仅作为**辅助先验**，不替代几何标定
- 仅用于**图像空间诊断**（如安全 ROI、遮挡判断、深度分层）；不得由 DA3 生成或混合反畸变像素
- 不直接用于3D坐标计算（三角化仍基于标定参数）
- 若需测试反畸变，必须使用独立、全局、可逆且完全由标定确定的映射；模型预测必须按同一映射回到原始鱼眼后才可参与几何计算

### 4. 检测框扩展策略

当前已有的框扩展方法：
- `build_upright_box_expansion_control.py`: 15%水平、10%顶部、25%底部扩展
- `local_perspective.py`: margin参数控制局部视角扩展（默认1.35）
- Sapiens2脚部伪标签导出使用45%的自适应margin

### 5. 实验分类

新实验脚本应归类到：
- `engineering_validation`: 工程验证
- `official_runs`: 正式基线实验
- `ablation_studies`: 消融实验

### 6. 文档要求

每个实验目录应包含：
- `EXPERIMENT.md`: 实验说明、唯一变量、成功标准、结论边界
- `run_metadata.json`: 完整的实验元数据
- `command.txt`: 重现命令

## 避免的模式

1. ❌ 不创建markdown文档除非明确要求
2. ❌ 不在没有独立人工标注的情况下声称"精度提升"
3. ❌ 不把模型输出的不一致称为"2D误差"
4. ❌ 不在README.md中过度使用emoji
5. ❌ 不把相对深度当作米制深度使用

## 当前可用的扩展点

### 检测框优化

- 已有YOLO检测框扩展机制
- 可基于每帧 DA3 深度在同一图像坐标内提出安全扩展候选，但最终框不得缩小冻结基线覆盖

### 图像预处理

- 已有鱼眼去畸变模块（`model_undistort.py`）
- 不使用 DA3 选择性去畸变或原图/反畸变图像素混合；这类输出没有单一可逆投影，不能进入双目几何链

### 关联与过滤

- 已有双目人体关联机制
- 人体关联仅使用冻结的二维观测与标定几何；不得用 DA3 相对深度作为额外关联约束

### 诊断与可视化

- 已有丰富的可视化工具
- 可添加DA3深度叠加显示，辅助分析失败案例

## 常用命令模式

### 双目采集回放

```bash
python .\run_stereo.py \
  --model yolo26x_pose \
  --stereo-capture-dir "path/to/capture" \
  --calibration .\calibration\results\stereo_fisheye.json
```

### 带扩展框的实验

参考 `build_upright_box_expansion_control.py` 的扩展规则。

### DA3深度分析

参考 `analyze_da3_at_reference_keypoints.py` 的采样和分析模式。
