# 策略：人工二维标注噪声与 Sapiens2 参考的定位（2026-09-03）

## 结论先行

"人工标注本身可能有较大误差，甚至不如 Sapiens2 的结果"是真实存在的风险，但**不是放弃人工标注的理由**。正确的做法是**测量并控制标注噪声**，而不是用同源模型输出替代人工参考。

Sapiens2 低置信度点可以人工矫正，但矫正后的 Sapiens2 **只能作为经人工复核的辅助参考**，不能作为真值，也**不能用于评估 Sapiens2 自身**（否则是循环评估）。

本文件记录这一判断及其依据，供后续冻结验证阶段沿用。它是对标注策略的约定，不是实验结论；不包含任何二维/三维准确率或模型优劣结论。

## 一、对"人工标注偏差可能不如 Sapiens2"的看法

1. **人工误差只在部分帧/关节上才是问题。** 在 far/mid 清晰帧上，1920×1080 原始鱼眼图中髋、膝、踝的解剖位置是明确可辨认的，单人点击误差通常远小于模型输出差异。人工误差最严重的场景恰恰是近距、鱼眼边缘、遮挡——而这些帧人眼本身也看不清。
2. **看不清就不硬标。** 对遮挡/边缘/模糊帧，正确处置是 `visibility=0 不标` 加类别化根因审核（参考 A1 标注协议的 visibility 规则），而不是给一个"看起来合理"的坐标。硬标不可见点会把人工噪声注入参考，其危害大于漏标。
3. **用标注者一致性定义"误差地板"。** 至少 20% 图像做第二位标注者复核，报告标注者间像素差（均值/中位数/P90）。任何"模型 A 比模型 B 好"的结论，若差值小于标注者间差异，都不能成立。这条规则把"人工噪声"从模糊担忧变成可量化、可报告的边界。
4. **结论口径必须写"一致性"而非"准确率"。** 只要参考不是独立人工真值，所有数值一律表述为"与参考的一致性"，参考的噪声由双人复核结果显式标注。这是本项目既有纪律的延续。

## 二、对"矫正 Sapiens2 低置信点"的看法

**可以矫正，但有严格前提。**

1. **盲标**：矫正时只看原始鱼眼图与解剖结构，不看 PMPose/ProbPose 输出、不看三角化/重投影结果、不看 Sapiens2 自身置信度之外的任何模型信息。
2. **只矫正可见且明显偏离解剖位置的点**：把矫正值单独写入新列，保留 Sapiens2 原始值、矫正原因（anatomical / occlusion / edge / unchanged），不允许静默替换。
3. **角色必须分开**：
   - 若 Sapiens2 是三个被比较模型之一（PMPose / ProbPose / Sapiens2），则**矫正后的 Sapiens2 不能作为评估 Sapiens2 的参考**（循环）；它只能作为 PMPose / ProbPose 的相对参考，结论写成"与经人工复核的 Sapiens2 参考的一致性"。
   - 若要评估 Sapiens2 自身，必须用完全独立的人工坐标，不能用自己的矫正版本。
4. **定位**：矫正后的 Sapiens2 参考在严谨性上弱于全人工标注，强于未矫正伪标签。它适合作为**辅助筛查**，不适合作为**唯一真值来源**。

## 三、推荐的阶段化标注路线（贴合单一标注者现实）

1. **先做类别化审核（抗噪、决定可用帧集）**：目标人身份审核 + 高重投影失败点根因审核。这两项不需要精确像素，只需要类别判断（正确/错位/遮挡/边缘/疑似几何），受人工噪声影响最小，却能直接决定哪些帧可用于精确标注、哪些帧应排除。
2. **再在明确可见的核心帧上精确标注**：髋/膝/踝共 6 点，优先覆盖 far/mid 清晰帧；近距清晰帧按可见性标注，模糊帧 `visibility=0`。脚趾/脚跟留到 v2，不与 COCO-17 下肢混标。复用既有 960 行 A1 模板（其中脚趾行保留为后续专项），与"120 图 × 6 点"方案等价且不产生新格式。
3. **Sapiens2 矫正仅作辅助**：仅用于筛查"Sapiens2 参考中明显偏离解剖的点是否也影响了模型比较"，不作为主参考。
4. **人力不足时的科学回退**：
   - 保留 Sapiens2 伪标签作为相对参考；
   - 另做 20% 抽样的独立人工复核，输出"参考噪声报告"（人工估计偏差带）；
   - 所有一致性结论都带上该误差带。
   - 这是诚实且可发表的路线；**最不可取的是一边声称"独立人工真值"，一边大量依赖 Sapiens2 输出**。

## 四、明确不做

- 不让 Sapiens2 承担真值角色。
- 不因"人工有噪声"而放弃标注。
- 不把补全、平滑、滤波当作去噪手段（错误 3D + 平滑 = 漂亮的错误）。
- 不新增模型、DA3、ROI 调参或三维可视化作为现阶段主投入。

## 边界

本文件是标注策略约定，不是实验记录；不产生任何新的测量数值，也不修改既有标注模板、预测、标定、阈值或实验结果。
